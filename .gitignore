node_modules/
dist/
.turbo/
*.tsbuildinfo
.env
.env.local
coverage/

# local blob store / preview scratch
.scholia/
.blobstore/
# `scholia share` site-link marker (a zip share drops one in cwd; never commit it)
.scholia

# playwright e2e artifacts
playwright-report/
test-results/
.last-run.json

# vitest JSON reporter output (written by .github/workflows/check.yml test-hosted job)
results.json

# agent-tool scratch (own node_modules, session plans, page/console dumps)
.opencode/
.playwright-cli/

# editor / os
.DS_Store
.idea/
.vscode/
