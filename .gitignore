node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/

# local blob store / preview scratch
.collab/
.blobstore/
# `collab share` site-link marker (a zip share drops one in cwd; never commit it)
.collab

# playwright e2e artifacts
playwright-report/
test-results/
.last-run.json

# agent-tool scratch (own node_modules, session plans, page/console dumps)
.opencode/
.playwright-cli/

# editor / os
.DS_Store
.idea/
.vscode/
