---
"scholia": patch
---

Configure automated releases. Adds Changesets (versioning, changelog, CI
changeset gate) and an npm trusted-publishing release workflow (OIDC, no
long-lived token). No CLI behaviour change.
