---
"scholia": patch
---

Fix a Reaction to a Comment hiding the rest of the fixed palette instead of adding to it — the rail now shows tallies alongside a single add-reaction chip that reveals the remaining palette on demand, cutting an unreacted Comment's tab stops from six to one. Reaction chips also carry `aria-pressed`, and their tooltip now names who reacted instead of just a count.
