---
"scholia": patch
---

Fix the article column collapsing below a readable floor in Local Preview — the multi-column grid now floors the article track at `--layout-content-min-width`, so Nav/Outline/rail can no longer squeeze the reading column at intermediate viewport widths and the page scrolls horizontally instead.
