---
"scholia": patch
---

Improve the comment composer: autofocus the textarea, add Escape/Cmd+Enter keyboard shortcuts, render the quoted passage in the header, position floating affordances below the selection, and render page-level composers inline at the top of the rail.
