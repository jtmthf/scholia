---
"scholia": patch
---

Fix the comment rail clipping Conversations past a viewport's worth in Local Preview — the rail's sticky box is now its own scroll container, so every Conversation stays reachable.
