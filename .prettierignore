pnpm-lock.yaml
CHANGELOG.md

# Generated or vendored — see .gitignore for the rest; Prettier reads it too.
**/dist/
**/*.d.ts

# Fixtures are formatted the way the test asserts on them.
e2e/fixtures/
packages/*/test/fixtures/
packages/*/tests/fixtures/

# Written by `pnpm --filter scholia skill`, byte-compared against the generator
# by packages/cli/test/skill.test.ts — formatting it would only ever make it stale.
packages/cli/skills/
